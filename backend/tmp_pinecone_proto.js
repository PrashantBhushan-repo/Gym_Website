import dotenv from "dotenv"; dotenv.config({ path: "./.env" }); import { Pinecone } from "@pinecone-database/pinecone"; const pc = new Pinecone({ apiKey: process.env.PINECONE_API_KEY, environment: process.env.PINECONE_ENVIRONMENT }); console.log("Pinecone prototype methods:", Object.getOwnPropertyNames(Object.getPrototypeOf(pc)).sort()); const index = pc.index("gym-knowledge"); console.log("Index prototype methods:", Object.getOwnPropertyNames(Object.getPrototypeOf(index)).sort());
